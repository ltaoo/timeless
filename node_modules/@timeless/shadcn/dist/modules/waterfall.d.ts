import { ViewProps, TimelessElement } from '@timeless/headless';
import { WaterfallModel, WaterfallCellModel } from '@timeless/ui';
export declare function Waterfall<T extends Record<string, unknown>>(props: ViewProps & {
    store: WaterfallModel<T>;
    render: (payload: T, cell: WaterfallCellModel<T>) => TimelessElement;
}): TimelessElement;
