export function SidebarViewModel(props) {


  // // 绑定事件
  // container.querySelectorAll(".nav-item").forEach((item) => {
  //   item.addEventListener("click", () => {
  //     const view = item.dataset.view;
  //     this.setView(view);
  //     this.onNavigate(view);
  //   });
  // });
  return {

  };
}
