# ShadcnUI 组件库使用手册

基于「控制器(Core/Store)」驱动视图的 Web 端组件库。无框架依赖，纯 vanilla JS。

## 核心理念

视图组件是**无状态的渲染层**，所有状态和行为由 Core（控制器）持有。视图通过 `store` prop 接收控制器，将用户交互委托给 store 的方法，并通过 `store.onStateChange` 响应状态变化重新渲染。

```
Core（逻辑层）  ──state──>  View（渲染层）
Core（逻辑层）  <──event──  View（渲染层）
```

## 全局变量约定

`shadcnui.js` 将所有组件和工具函数挂载到 `window`（如 `ref`, `computed`, `View`, `Txt`, `Show`, `For`, `Button`, `Dialog` 等）。

编写 UI 组件文件时**不需要 import**，直接使用全局变量即可。原因：组件文件虽然被 `shadcnui.js` 通过 ES module import，但函数体在被调用时才执行，此时全局变量已挂载完毕。

## 响应式原语

```js
// ref：可变响应值，赋值自动通知订阅者
const count = ref(0);
count.value = 1;

// ref 支持数组，push/pop/splice 等操作自动触发增量更新
const list = ref([]);
list.value.push(item); // 触发 { type: "insert", index, items }

// computed：依赖 ref 自动重算
const double = computed({ count }, (d) => d.count * 2);

// classnames：响应式 CSS 类名管理
const cn = classnames("base-class");
cn.add("active");
cn.del("active");
```

## 视图组件接口

每个组件是一个函数，签名为 `Component(props, children?) -> ViewReturn`。

`ViewReturn` 对象必须包含：
- `$elm` - DOM 元素
- `render()` - 返回 DOM 元素，挂载子节点
- `onMounted()` - 挂载后回调
- `onUnmounted()` - 卸载时回调，**必须在此清理事件订阅**

## 编写 store 驱动的视图组件的标准模式

以 `Button` 为参考实现：

```js
export function MyComponent(props, children) {
  const { store, class: cn, ...rest } = props;

  // 1. 用 ref 包装 store.state，作为响应式数据源
  const state = ref(store.state);

  // 2. 订阅 store 状态变化，收集取消订阅函数
  const events = [];
  events.push(store.onStateChange(() => {
    state.value = store.state;
  }));

  // 3. 用 computed 派生响应式 class/内容
  // 4. 在 View 的事件回调中调用 store 方法
  // 5. 在 onUnmounted 中清理订阅
  return View({
    ...rest,
    class: computed({ state }, (d) => {
      return ["base-classes", d.state.active ? "active" : "", cn].filter(Boolean).join(" ");
    }),
    onClick() { store.doSomething(); },
    onUnmounted() {
      for (const fn of events) if (typeof fn === "function") fn();
    },
  }, children);
}
```

**关键规则：**
- 视图**不持有业务状态**，只从 `store.state` 读取
- 视图**不直接修改状态**，只调用 `store` 的方法（如 `store.click()`、`store.toggle()`、`store.setValue()`）
- `onStateChange` 返回的取消订阅函数**必须在 `onUnmounted` 中调用**

## 内置组件速查

### 基础视图

| 组件 | 用途 | store 类型 |
|------|------|-----------|
| `View(props, children)` | 通用容器，默认 `<div>` | 无 |
| `Txt(stringOrRef)` | 文本节点，支持 ref 自动更新 | 无 |

### 控制流

| 组件 | 用途 |
|------|------|
| `Show({ when: computedBool }, children)` | 条件渲染，`when` 必须是 ref/computed |
| `For({ each: refArray, render(item, i) })` | 列表渲染，`each` 支持 ref 数组增量更新 |
| `Match` | 多分支条件渲染 |

### 带 store 的组件

| 组件 | store 类型 | 核心方法 |
|------|-----------|---------|
| `Button` | `ButtonCore` | `click()`, `disable()`, `enable()`, `setLoading()` |
| `Input` | `InputCore` | `setValue()` |
| `Checkbox` | `CheckboxCore` | `toggle()`, `check()`, `uncheck()` |
| `Select` | `SelectCore` | `select(value)` |
| `Dialog` | `DialogCore` | `show()`, `hide()`, `ok()`, `cancel()` |
| `Toggle` | `ToggleCore` | `toggle()` |
| `Tabs` | `TabsCore` | `selectTab(value)` |
| `DropdownMenu` | `DropdownMenuCore` | `toggle()`, `hide()` |
| `Slider` | `SliderCore` | - |
| `Progress` | `ProgressCore` | - |
| `Toast` | `ToastCore` | - |

### 浮层相关

| 组件 | 用途 |
|------|------|
| `Portal(props, children)` | 将子节点挂载到 `document.body` |
| `Presence({ store }, children)` | 控制进出场动画的条件渲染 |
| `Popper({ store }, children)` | 浮动定位层 |
| `Popover` | 弹出气泡 |

### 展示组件（无 store）

`Flex`, `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `Badge`, `Separator`, `Avatar`, `Skeleton`, `Tooltip`, `Alert`, `ScrollArea`, `Sheet`, `Accordion`, `Table` 系列, `Head2`, `Paragraph`, `AspectRatio`

这些组件只接收 props 和 children，不需要 store。

## 完整示例：页面组合

```js
// 业务层：创建控制器
const submitBtn = new ButtonCore({ _name: "submit" });
const nameInput = new InputCore({ placeholder: "请输入名称" });
const confirmDialog = new DialogCore();

submitBtn.onClick(() => {
  confirmDialog.show();
});
confirmDialog.onOk(() => {
  submitBtn.setLoading(true);
  // ... 发起请求
});

// 视图层：用控制器驱动组件
const page = View({ class: "p-4 space-y-4" }, [
  Input({ store: nameInput }),
  Button({ store: submitBtn, variant: "default" }, [Txt("提交")]),
  Dialog({ store: confirmDialog }, [
    Txt("确认提交吗？"),
  ]),
]);

// 挂载
document.getElementById("app").appendChild(page.render());
page.onMounted();
```

## View props 参考

```js
View({
  type: "div",          // HTML 标签名
  id: "my-id",          // 支持 ref
  class: "cls",         // 支持 string | ref | classnames 对象
  style: "color:red",   // 支持 string | ref
  dataset: { key: "v" },// data-* 属性
  onClick(event) {},
  onFocus(event) {},
  onBlur(event) {},
  onMounted($elm) {},
  onUnmounted() {},
}, children)
```

## Core 类型定义

所有 Core 类的完整类型签名见 `frontend/types/timeless.core.d.ts`。
