{
  "name": "@timeless/unittest",
  "version": "0.31.1",
  "private": true,
  "description": "Cross-component integration tests for Timeless headless UI — asserts VM state without DOM",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@timeless/inner-vm": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
