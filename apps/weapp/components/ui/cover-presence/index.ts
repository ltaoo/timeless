import {  PresenceCore  } from "@timeless/domains";, store.state, store.pending);
      store.handleAnimationEnd();
      // if (store.visible) {
      //   return;
      // }
      // store.unmount();
    },
  },
});
