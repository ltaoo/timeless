import { VariantProps, cva } from "class-variance-authority";

import {  ButtonCore  } from "@timeless/domains";, store);
      if (!store) {
        return;
      }
      store.click();
    },
  },
});
