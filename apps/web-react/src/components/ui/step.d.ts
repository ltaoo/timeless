import React from "react";
import { StepCore } from "@/domains/step";
export declare const StepSwitch: React.MemoExoticComponent<(props: {
    store: StepCore;
    options: Record<number, React.ReactNode>;
} & React.AllHTMLAttributes<HTMLDivElement>) => React.JSX.Element>;
