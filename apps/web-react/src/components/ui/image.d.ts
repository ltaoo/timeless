import React from "react";
import { ImageCore } from "@/domains/ui/image";
export declare const LazyImage: React.MemoExoticComponent<(props: {
    store: ImageCore;
    alt?: string;
} & React.HTMLAttributes<HTMLImageElement>) => React.JSX.Element>;
