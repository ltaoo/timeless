import React from "react";
export declare const Show: React.MemoExoticComponent<(props: {
    when: boolean;
    fallback?: React.ReactElement;
    children: React.ReactNode;
}) => React.JSX.Element>;
