import React from "react";
import { PlayerCore } from "@/domains/player/index";
export declare const PlayerProgressBar: React.MemoExoticComponent<(props: {
    store: PlayerCore;
}) => React.JSX.Element>;
