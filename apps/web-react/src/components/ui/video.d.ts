/**
 * @file 播放器
 */
import React from "react";
import { PlayerCore } from "@/domains/player";
export declare const Video: React.MemoExoticComponent<(props: {
    store: PlayerCore;
}) => React.JSX.Element>;
