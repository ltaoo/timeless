/**
 * @file 用户注册
 */
import { ViewComponent, ViewComponentProps } from "~/store/types";
import { useViewModel } from "~/hooks";
import { Button, Input } from "~/components/ui";

import {  BizError  } from "@timeless/domains";>检测到当前已登录</div>
          </div>
        ) : null}
      </div>
    </div>
  );
};
