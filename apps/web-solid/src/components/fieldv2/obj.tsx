import { JSX } from "solid-js/jsx-runtime";
import { createSignal, Show } from "solid-js";

import { useViewModelStore } from "~/hooks";

import {  ObjectFieldCore, SingleFieldCore  } from "@timeless/kit";]: true,
              }}
            >
              {props.children}
            </div>
          </div>
        </div>
      </div>
    </Show>
  );
}
