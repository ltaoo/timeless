import {  PresenceCore  } from "@timeless/kit";, store.state, store.pending);
      store.handleAnimationEnd();
      // if (store.visible) {
      //   return;
      // }
      // store.unmount();
    },
  },
});
