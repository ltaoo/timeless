# Flutter Todo App

## 运行方式

1. 创建 Flutter 项目: `flutter create todo_app`
2. 将 `todo_app.dart` 复制到 `lib/`
3. 运行项目: `flutter run`

## 功能

- 输入框 + 添加按钮
- 可滚动 todo 列表（checkbox + title）
- 选中时 title 显示删除线
- 选中时显示归档按钮，点击移除 todo
